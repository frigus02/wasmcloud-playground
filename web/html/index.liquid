<!DOCTYPE html>
<html>
<head>
	<title>wasmcloud Playground</title>
</head>
<body>
	<h1>Todos</h1>
	<ul>
		{% for todo in todos %}
			<li>{{ todo.id }} {{ todo.title }} {{ todo.completed }}</li>
		{% endfor %}
	</ul>
</body>
</html>
